node_modules/
dist/
# Pyfa headless oracle for differential parity testing — cloned + venv + built
# eve.db (~100MB), reproduced by scripts/setup-pyfa-oracle.sh. Never committed.
.pyfa/
*.log
.DS_Store
.idea/
.vscode/
*.tsbuildinfo
# Raw SDE source staged by scripts/fetch-sde.mjs — large + CCP-owned, ephemeral.
# (The FILTERED bundle under data/ IS committed/shipped; the raw source is not.)
/.sde-src/
/fixtures/sde/
/*.jsonl
